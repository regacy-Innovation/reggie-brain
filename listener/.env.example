# Keep real Slack credentials in listener/.env, which is ignored by Git.
SLACK_BOT_TOKEN=replace-me
SLACK_APP_TOKEN=replace-me
SLACK_SIGNING_SECRET=replace-me
PERMITTED_SLACK_CHANNEL_ID=C074BCJGQGP
# Optional. Defaults to the repository root containing this listener.
REGGIE_BRAIN_PATH=C:/Reggie/brain
# Optional local machine paths. Use any absolute paths appropriate to this
# computer. Per-project paths in config/projects.local.yml take precedence.
# REGGIE_PROJECT_CLONE_ROOT=/Users/example/Reggie/projects
# REGGIE_WORKTREE_ROOT=/Users/example/Reggie/worktrees
# REGGIE_STATE_ROOT=/Users/example/Reggie/state
# Optional. Defaults to `codex` on PATH.
CODEX_BIN=codex
